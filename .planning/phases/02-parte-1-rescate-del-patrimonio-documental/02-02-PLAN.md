---
phase: 02-parte-1-rescate-del-patrimonio-documental
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb
autonomous: false
requirements: [P1-01, P1-03, P1-06]
must_haves:
  truths:
    - "Cells 5, 6 imprimen dimensiones m=16384, n=55 y un κ(A^T A) >> 1/eps_maq (>10^15) cuando se ejecutan"
    - "Cell 9 entra en el bloque except O imprime una solución que se visualiza colapsada en cell 10"
    - "qr_mgs (cell 14) y qr_householder (cell 16) producen Q_hat y R_hat sin errores tras Restart & Run All"
    - "Cell 18 ejecuta back_substitution sin AttributeError ni ImportError; cell 19 muestra 4 paneles con imágenes restauradas legibles via MGS y Householder"
    - "Cell 20 imprime ‖Q_hat^T Q_hat - I‖_2 para MGS y Householder, y el valor de Householder es estrictamente menor (varios órdenes de magnitud) que el de MGS"
    - "Cell 0 (Markdown integrantes) tiene un TODO visible recordando a los tres integrantes (Tomás Gallón, Sebastian Montoya, Miguel Gomez) que añadan sus códigos manualmente antes de la entrega"
  artifacts:
    - path: "Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb"
      provides: "Notebook ejecutado con todos los outputs visibles para Parte 1"
      contains: "execution_count"
  key_links:
    - from: "cell 18 (back_substitution)"
      to: "cell 19 (visualización)"
      via: "variables restaurada_mgs, restaurada_hh consumidas por axes[2].imshow / axes[3].imshow"
      pattern: "restaurada_(mgs|hh)"
    - from: "cell 20 (orthogonality norms)"
      to: "cell 21 (conclusión)"
      via: "valores numéricos de ort_mgs y ort_hh respaldando la afirmación de cell 21"
      pattern: "ort_(mgs|hh)"
---

<objective>
Verificar que las correcciones del plan 02-01 producen un notebook ejecutable de extremo a extremo para Parte 1, y agregar el TODO de códigos de estudiante en cell 0.

Purpose: Cerrar Phase 2 confirmando que cells correctas (P1-01, P1-03, P1-06) siguen funcionando, que las cells modificadas (P1-02, P1-04, P1-05 — verificadas en plan 02-01) integran correctamente con el resto del notebook, y que el placeholder de códigos de estudiante está marcado para la entrega.

Output: Notebook con outputs visibles para Parte 1 (cells 0–21), TODO de códigos en cell 0, y human-verify checkpoint pasado.
</objective>

<execution_context>
@C:/Users/Tomas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md
@.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md
@.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-01-SUMMARY.md

<interfaces>
<!-- Cells correctas que NO se modifican pero cuyas salidas se verifican -->

cell 5 (build A): produce variable global `A` con `A.shape == (16384, 55)`. Imprime dimensiones.
cell 6 (cond AtA): imprime `cond_AtA` (numpy.float64), debería ser >> 1e15 (cercano a 1/eps_maq o mayor).
cell 9 (try-except normal eq): define `c_normal` o setea `exito_normal = False`.
cell 10 (visualize collapse): produce subplot con imagen colapsada o mensaje de fallo.
cell 14 (qr_mgs): retorna `Q (m,n)`, `R (n,n)`.
cell 19 (4-panel visualization): consume `imagen_danada`, `restaurada_normal` (si existe), `restaurada_mgs`, `restaurada_hh`. Output: figure con 4 axes.
cell 20 (ortogonalidad): imprime `ort_mgs` y `ort_hh`. Espera: `ort_hh < ort_mgs` por varios órdenes de magnitud.
cell 21 (Markdown conclusión): texto fijo, no se evalúa.

cell 0 (Markdown integrantes): contiene la fila de tabla con `*(código)*` como placeholder para cada integrante.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar TODO explícito de códigos de estudiante en cell 0</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (estado actual de cell 0)
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md (decisión "Códigos de estudiante")
  </read_first>
  <action>
    Modificar cell 0 (Markdown integrantes) del notebook para incluir un comentario HTML visible como TODO. Localizar la tabla Markdown que contiene las filas con `*(código)*` para los tres integrantes (Tomás Gallón, Sebastian Montoya, Miguel Gomez) y AÑADIR — inmediatamente después de la tabla, separado por una línea en blanco — la siguiente línea exacta:

    ```markdown
    > **TODO antes de la entrega (2026-05-05):** Tomás Gallón, Sebastian Montoya y Miguel Gomez deben reemplazar cada `*(código)*` en la tabla anterior por su código de estudiante / cédula real. Este aviso debe eliminarse una vez completados los tres códigos.
    ```

    No tocar la tabla en sí (los `*(código)*` permanecen como placeholders hasta que los integrantes los reemplacen manualmente). No tocar otras secciones de cell 0 (encabezado del curso, profesor, semestre).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const nb=JSON.parse(fs.readFileSync('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb','utf8')); const s=Array.isArray(nb.cells[0].source)?nb.cells[0].source.join(''):nb.cells[0].source; if(!s.includes('TODO antes de la entrega (2026-05-05)')) throw new Error('cell 0 falta marker TODO'); if(!s.includes('Tomás Gallón')) throw new Error('cell 0 falta Tomás Gallón en TODO'); if(!s.includes('Sebastian Montoya')) throw new Error('cell 0 falta Sebastian Montoya'); if(!s.includes('Miguel Gomez')) throw new Error('cell 0 falta Miguel Gomez'); if(!s.includes('*(código)*')) throw new Error('cell 0 perdio el placeholder *(código)* en la tabla'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - cell 0 contiene la cadena literal `TODO antes de la entrega (2026-05-05)`
    - cell 0 contiene los tres nombres: `Tomás Gallón`, `Sebastian Montoya`, `Miguel Gomez`
    - cell 0 todavía contiene la cadena `*(código)*` (los placeholders permanecen para que los integrantes los reemplacen)
    - cell 0 sigue siendo `cell_type: markdown`
    - Notebook nbformat-válido
  </acceptance_criteria>
  <done>
    cell 0 tiene un blockquote `> **TODO ...**` después de la tabla de integrantes recordando a los tres integrantes que añadan sus códigos antes del 2026-05-05. Los placeholders `*(código)*` permanecen intactos en la tabla.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restart & Run All — ejecutar todo el notebook headless y verificar que Parte 1 corre sin errores</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (notebook completo después de plan 02-01 y task 1 de este plan)
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md (sección "Notebook Cell Map (Parte 1)" para entender qué outputs esperar en cada cell)
  </read_first>
  <action>
    Ejecutar el notebook completo de manera headless usando `jupyter nbconvert --to notebook --execute --inplace` (o equivalente vía `jupyter execute`). Comando exacto:

    ```bash
    jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=600 Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb
    ```

    Si el comando falla por algún error de Parte 2 (cells 22+), el plan está limitado a Parte 1 — pero como cell 32 fue corregida en plan 02-01, no debería fallar. Si Parte 2 falla por un error pre-existente NO relacionado con plan 02-01 (ej. NASA URL inaccesible), documentar el error en el SUMMARY y continuar — la verificación de Parte 1 debe completarse de todos modos.

    Verificaciones programáticas tras la ejecución:

    1. Cells 3, 5, 6, 9, 10, 14, 16, 18, 19, 20 deben tener `execution_count` no-null y outputs no vacíos.
    2. Cell 6 debe imprimir un valor de `cond_AtA` que cumpla `cond_AtA > 1e15` (extraer del output text/stream).
    3. Cell 18 debe ejecutar sin excepciones (verificar que no hay `output_type: error` en cell 18).
    4. Cell 19 debe contener al menos un output de tipo `display_data` con un `image/png` (la figura 4-panel).
    5. Cell 20 debe imprimir `ort_mgs` y `ort_hh`. Extraer ambos valores y verificar que `ort_hh < ort_mgs / 100` (Householder al menos 2 órdenes de magnitud mejor — esto es típicamente 1e-15 vs 1e-9 o peor).
    6. Cell 18 NO debe tener trazas de `solve_triangular` en su output ni en su source.

    Si cualquier verificación falla, NO continuar al checkpoint humano. Reportar el error.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=600 Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb && node -e "const fs=require('fs'); const nb=JSON.parse(fs.readFileSync('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb','utf8')); const idxs=[3,5,6,9,10,14,16,18,19,20]; for (const i of idxs){ const c=nb.cells[i]; if(c.execution_count==null) throw new Error('cell '+i+' sin execution_count'); if(!c.outputs||c.outputs.length===0) throw new Error('cell '+i+' sin outputs'); for(const o of c.outputs){ if(o.output_type==='error') throw new Error('cell '+i+' tuvo error: '+(o.ename||'?')); } } const c18=nb.cells[18]; const c18src=Array.isArray(c18.source)?c18.source.join(''):c18.source; if(c18src.includes('solve_triangular')) throw new Error('cell 18 todavia menciona solve_triangular'); const c19=nb.cells[19]; const hasImg=c19.outputs.some(o=>o.data&&o.data['image/png']); if(!hasImg) throw new Error('cell 19 no produjo image/png'); const c20=nb.cells[20]; const c20text=c20.outputs.map(o=>o.text?(Array.isArray(o.text)?o.text.join(''):o.text):'').join(''); if(!/ort_mgs|MGS/i.test(c20text)) throw new Error('cell 20 output no menciona MGS'); if(!/ort_hh|Householder/i.test(c20text)) throw new Error('cell 20 output no menciona Householder'); console.log('Notebook ejecutado OK; outputs presentes; cell 18 limpio de solve_triangular; cell 19 produjo imagen; cell 20 imprimió ambas normas.');"</automated>
  </verify>
  <acceptance_criteria>
    - Comando `jupyter nbconvert ... --execute --inplace` retorna exit code 0
    - cell 3 tiene `execution_count` no-null y output con imagen (subplot original + dañada)
    - cell 5 tiene output que contiene las cadenas `m = 16384` y `n = 55` (o equivalente — dimensiones impresas)
    - cell 6 tiene output que contiene un valor numérico para κ(A^T A) — el valor extraído debe ser > 1e15
    - cell 9 ejecutó sin lanzar excepción no-capturada (try-except funciona; si entró al except eso también es válido)
    - cell 10 produjo display_data con `image/png` O imprimió mensaje de fallo (ambos aceptables — cell 9 puede haber fallado dentro del except)
    - cell 14 ejecutó sin error
    - cell 16 ejecutó sin error (definición de qr_householder con sign-fix)
    - cell 18 ejecutó sin error y NO contiene la cadena `solve_triangular` en su source post-ejecución
    - cell 19 produjo `display_data` con `image/png` (la figura 4-panel)
    - cell 20 imprimió outputs que mencionan tanto MGS como Householder con valores numéricos para `ort_mgs` y `ort_hh`
    - Ningún output de tipo `error` en cells 0–21
  </acceptance_criteria>
  <done>
    Notebook ejecutado con `jupyter nbconvert --execute --inplace`, exit code 0. Cells 0–21 (Parte 1) tienen outputs completos. Cell 18 corre sin scipy.linalg.solve_triangular. Cell 19 muestra la figura 4-panel. Cell 20 imprime ambas normas de ortogonalidad y Householder es órdenes de magnitud mejor que MGS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — verificar visualmente outputs de Parte 1 y confirmar TODO de códigos</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (después de Task 2; ya tiene outputs ejecutados)
  </read_first>
  <what-built>
    - Plan 02-01 aplicó tres correcciones surgicales (sign fix en cells 16 y 32, back_substitution manual en cell 18, expansión del análisis κ²en cell 11)
    - Plan 02-02 Task 1 agregó TODO de códigos en cell 0
    - Plan 02-02 Task 2 ejecutó Restart & Run All headless; todas las cells de Parte 1 (0–21) tienen outputs
    - Verificaciones programáticas confirmaron: ort_hh << ort_mgs, cell 18 sin scipy, cell 19 produjo figura 4-panel
  </what-built>
  <how-to-verify>
    Abrir `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` en Jupyter Lab/Notebook o VS Code y verificar visualmente:

    1. **Cell 0 (Integrantes):** Confirmar que aparece el blockquote `> **TODO antes de la entrega (2026-05-05):** ...` recordando a los tres integrantes añadir sus códigos. Los `*(código)*` siguen presentes.

    2. **Cell 6 (output):** Leer el valor numérico impreso de κ(A^T A). Debe ser un número del orden 10^15 o mayor (el research dice "típicamente 10^17–10^21 para esta matriz Hilbert-like"). Confirmar que el output también imprime el cociente κ(A^T A) / (1/eps_maq) o similar evidencia de colapso.

    3. **Cell 7 (análisis Inciso 1):** Markdown intacto, no modificado.

    4. **Cell 10 (output):** Confirmar visualmente que la imagen producida por las ecuaciones normales está colapsada / ilegible (esperado: ruido o saturación, NO el documento legible).

    5. **Cell 11 (análisis Inciso 2 — AMPLIADO):** Confirmar que la celda contiene los DOS párrafos originales más un TERCER párrafo nuevo con: la fórmula κ(A^T A) = [κ(A)]^2, la mención de "~16 dígitos" en precisión doble, "se pierden 30 dígitos" cuando κ(A^T A) ≈ 10^30, y "**ningún** dígito correcto".

    6. **Cell 18 (código):** Inspeccionar el source: NO debe tener `from scipy.linalg import solve_triangular`, debe tener `def back_substitution(R, b):` y dos llamadas a `back_substitution(...)`. Output debe imprimir "Factorizando A con MGS...", "Factorizando A con Householder...", "Listo." sin errores.

    7. **Cell 19 (figura 4-panel):** Visualizar la figura. Esperado: 4 subplots — (a) Documento Dañado, (b) Restauración con Ecuaciones Normales (colapsada/ilegible o "fallo"), (c) Restauración MGS (LEGIBLE — el texto histórico debe leerse claramente), (d) Restauración Householder (LEGIBLE — visualmente similar a MGS).

    8. **Cell 20 (output):** Leer los dos valores impresos. Confirmar que ‖Q^T Q − I‖_2 de Householder es **mucho** menor que el de MGS (al menos 3 órdenes de magnitud, típicamente Householder ~1e-14 y MGS ~1e-9 o peor para esta matriz mal condicionada).

    9. **Cell 21 (conclusión Inciso 4):** Markdown intacto, no modificado.

    10. **Cells 14, 15, 17:** Confirmar que NO fueron tocadas (qr_mgs, teoría Householder, header Inciso 4).

    Si cualquier elemento falla la inspección visual (especialmente: imagen 19c o 19d ilegible, ort_hh no menor que ort_mgs, cell 11 perdió contenido original, código de cell 18 retiene scipy), reportar el problema. Si todo se ve bien, aprobar.
  </how-to-verify>
  <action>
    Pausar la ejecución automática y solicitar al usuario humano que abra el notebook `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` en Jupyter Lab, Jupyter Notebook o VS Code, y que siga los 10 pasos descritos en <how-to-verify> arriba para validar visualmente cells 0, 6, 7, 10, 11, 14, 15, 17, 18, 19, 20, 21. El agente NO debe avanzar a otros planes hasta recibir el resume-signal.
  </action>
  <verify>
    <automated>echo "Checkpoint humano — verificación visual requerida; no hay verificación automática adicional. Ver acceptance_criteria."</automated>
  </verify>
  <acceptance_criteria>
    - Usuario humano confirma que las imágenes restauradas en cell 19 (paneles MGS y Householder) son legibles
    - Usuario confirma que ‖Q^T Q − I‖_2 de Householder es mucho menor que la de MGS
    - Usuario confirma cell 11 contiene los tres párrafos esperados (dos originales + el nuevo con κ²)
    - Usuario confirma cell 0 tiene el blockquote TODO de códigos
    - Usuario aprueba con texto literal "approved" o describe issues específicos
  </acceptance_criteria>
  <resume-signal>Type "approved" si todo se ve bien, o describe los problemas específicos encontrados (qué cell, qué se ve mal).</resume-signal>
  <done>
    Usuario aprobó visualmente: cells 19c y 19d muestran texto histórico legible; cell 20 confirma Householder >> MGS en estabilidad de ortogonalidad; cell 11 contiene la expansión κ²; cell 0 tiene TODO de códigos.
  </done>
</task>

</tasks>

<verification>
- `jupyter nbconvert --execute --inplace` exit code 0
- Outputs presentes en cells 0–21
- Cell 19 produjo figura 4-panel con 2 imágenes restauradas legibles
- Cell 20 muestra ort_hh << ort_mgs
- Cell 0 tiene TODO de códigos
- Usuario aprobó visualmente
</verification>

<success_criteria>
- Las 3 tasks completadas (TODO de códigos, Restart & Run All headless con verificaciones programáticas, human-verify aprobado)
- Phase 2 cierra: P1-01 a P1-06 todas verificadas (P1-02, P1-04, P1-05 implementadas en plan 02-01; P1-01, P1-03, P1-06 verificadas como correctas en este plan via execución exitosa)
- Notebook listo para Phase 3 (Parte 2 — Modelación Climática)
</success_criteria>

<output>
After completion, create `.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-02-SUMMARY.md` documentando:
- Outputs clave de Parte 1: valores numéricos de cond_AtA, ort_mgs, ort_hh
- Confirmación visual de imágenes restauradas legibles (paneles 19c, 19d)
- Estado del TODO de códigos en cell 0
- Aprobación humana
- Cualquier observación sobre Parte 2 si la ejecución headless reveló algo (ej. NASA URL accesible o no)
</output>
