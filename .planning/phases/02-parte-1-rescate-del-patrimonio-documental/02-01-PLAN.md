---
phase: 02-parte-1-rescate-del-patrimonio-documental
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb
autonomous: true
requirements: [P1-02, P1-04, P1-05]
must_haves:
  truths:
    - "qr_householder (cell 16) produce un reflector correcto incluso si x[0] == 0 (signo +1, no 0)"
    - "householder_qr (cell 32, Parte 2) tiene la misma corrección de signo aplicada"
    - "Cell 18 ya no importa scipy.linalg.solve_triangular ni la usa; resuelve Rc = Q^T b mediante back-substitution manual"
    - "Cell 11 menciona explícitamente κ(A^T A) = [κ(A)]^2 y cuantifica los ~16 dígitos de doble precisión y la pérdida de 30 dígitos cuando κ(A^T A) ≈ 10^30"
    - "qr_mgs (cell 14), visualización (cell 19), normas de ortogonalidad (cell 20) y conclusión Inciso 4 (cell 21) NO se modifican"
  artifacts:
    - path: "Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb"
      provides: "Notebook con las tres correcciones surgicales aplicadas"
      contains: "back_substitution"
  key_links:
    - from: "cell 18"
      to: "back_substitution helper"
      via: "llamada directa a back_substitution(R_mgs, Q_mgs.T @ b_p1) y back_substitution(R_hh, Q_hh.T @ b_p1)"
      pattern: "back_substitution\\("
    - from: "cell 16"
      to: "vector reflector v"
      via: "expresión (np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)"
      pattern: "\\(np\\.sign\\(x\\[0\\]\\) \\+ \\(x\\[0\\] == 0\\)\\)"
    - from: "cell 32"
      to: "vector reflector v (Parte 2)"
      via: "misma expresión corregida (np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)"
      pattern: "\\(np\\.sign\\(x\\[0\\]\\) \\+ \\(x\\[0\\] == 0\\)\\)"
---

<objective>
Aplicar tres correcciones surgicales al notebook `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` definidas en CONTEXT.md, sin disturbar las celdas que ya están correctas.

Purpose: Cerrar los gaps identificados en RESEARCH.md (sign edge case en Householder, prohibición de scipy.linalg.solve_triangular en Parte 1, expansión del análisis κ(A^T A) = [κ(A)]^2 con cuantificación de dígitos perdidos).

Output: Notebook con las cuatro celdas modificadas (11, 16, 18, 32). Todas las demás celdas intactas. Phase ready para verification (plan 02-02).
</objective>

<execution_context>
@C:/Users/Tomas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Tomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md
@.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md

<interfaces>
<!-- Funciones ya existentes en el notebook que las tareas deben usar/modificar -->

cell 14 (qr_mgs) — NO MODIFICAR. Firma:
```python
def qr_mgs(A):
    """Factorización QR reducida (thin) via Gram-Schmidt Modificado.
    Retorna Q_hat (m x n) con columnas ortonormales y R_hat (n x n) triangular superior."""
    return Q, R   # ambos numpy.ndarray
```

cell 16 (qr_householder) — MODIFICAR solo la línea del signo. Firma:
```python
def qr_householder(A):
    """Factorización QR reducida (thin) via Triangularización de Householder.
    Retorna Q_hat (m x n) y R_hat (n x n) triangular superior."""
    return Qt.T[:, :n], R[:n, :n]
```

cell 32 (householder_qr — Parte 2) — MODIFICAR solo la línea del signo. Firma:
```python
def householder_qr(A):
    """Factorización QR reducida via Triangularización de Householder.
    Retorna Q_hat (m x n) y R_hat (n x n)."""
    return Qt.T[:, :n], R[:n, :n]
```

cell 18 (resolución QR) — REEMPLAZAR completo. Variables globales disponibles:
- `A` (numpy ndarray, 16384 x 55) — definida en cell 5
- `b_p1` (numpy ndarray, 16384,) — definida en cell 9 como `imagen_danada.ravel()`
- `imagen_danada` — definida en cell 3
- `alto`, `ancho` — definidas en cell 3
- `qr_mgs`, `qr_householder` — definidas en cells 14 y 16

Salidas que cell 18 debe producir (consumidas por cell 19):
- `Q_mgs`, `R_mgs`, `c_mgs`, `superficie_mgs`, `restaurada_mgs`
- `Q_hh`,  `R_hh`,  `c_hh`,  `superficie_hh`,  `restaurada_hh`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir el edge case de signo en qr_householder (cell 16) y householder_qr (cell 32)</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (estado actual de cells 16 y 32)
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md (decisión "Corrección del edge case en Householder")
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md (sección "Pattern: Sign Edge-Case Fix for Householder" y "Pitfall 2")
  </read_first>
  <action>
    Modificar el notebook JSON `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` aplicando exactamente la misma sustitución de una línea en DOS celdas:

    1. En **cell 16** (definición de `qr_householder`), localizar la línea:
       ```python
       v[0] += np.sign(x[0]) * np.linalg.norm(x)
       ```
       Reemplazarla por:
       ```python
       v[0] += (np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)
       ```

    2. En **cell 32** (definición de `householder_qr`, Parte 2), localizar la MISMA línea:
       ```python
       v[0] += np.sign(x[0]) * np.linalg.norm(x)
       ```
       Reemplazarla por:
       ```python
       v[0] += (np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)
       ```

    Adicionalmente, en cell 16 actualizar el comentario adyacente (la línea inmediatamente anterior que dice "Vector estable de reflexión: v = sign(x_1)*||x||*e_1 + x") para reflejar el caso de borde:
    ```
    # Vector estable de reflexión: v = (sign(x_1) + [x_1 == 0]) * ||x|| * e_1 + x
    # El término (x_1 == 0) garantiza signo +1 cuando np.sign(0) devuelve 0.0,
    # evitando que v[0] se anule y rompa el reflector.
    ```
    Hacer la misma actualización del comentario en cell 32.

    NO modificar ninguna otra línea de cells 16 o 32. NO tocar cells 14, 15, 17, 19, 20, 21 ni ninguna otra. Usar un editor de notebooks o manipulación directa del JSON (no ejecutar el notebook todavía).

    Implementación recomendada: Leer el `.ipynb` como JSON con `json.load`, mutar `cells[16]['source']` y `cells[32]['source']` (que son listas de strings, una por línea — preservar las terminaciones de línea), y escribir con `json.dump(..., indent=1, ensure_ascii=False)`. Verificar después con `nbformat` o `jupyter nbconvert --to script` que el JSON sigue siendo notebook válido.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const nb=JSON.parse(fs.readFileSync('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb','utf8')); const s16=Array.isArray(nb.cells[16].source)?nb.cells[16].source.join(''):nb.cells[16].source; const s32=Array.isArray(nb.cells[32].source)?nb.cells[32].source.join(''):nb.cells[32].source; const need='(np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)'; const bad='np.sign(x[0]) * np.linalg.norm(x)'; if(!s16.includes(need)) throw new Error('cell 16 missing fix'); if(!s32.includes(need)) throw new Error('cell 32 missing fix'); if(s16.includes('np.sign(x[0]) * np.linalg.norm(x)')) throw new Error('cell 16 still has buggy line'); if(s32.includes('np.sign(x[0]) * np.linalg.norm(x)')) throw new Error('cell 32 still has buggy line'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` cell 16 contiene la cadena exacta `(np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)`
    - `Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` cell 32 contiene la cadena exacta `(np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)`
    - cell 16 NO contiene la cadena `np.sign(x[0]) * np.linalg.norm(x)` (la versión sin paréntesis adicionales)
    - cell 32 NO contiene la cadena `np.sign(x[0]) * np.linalg.norm(x)`
    - cells 14, 15, 17, 19, 20, 21 son byte-idénticas a su estado previo (verificable con `git diff` mostrando cambios solo en cells 16 y 32 — y en cell 11 y 18 después de tasks 2 y 3)
    - `python -c "import nbformat; nbformat.read('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb', 4)"` ejecuta sin error
  </acceptance_criteria>
  <done>
    Ambas funciones Householder (qr_householder cell 16 y householder_qr cell 32) usan la fórmula `(np.sign(x[0]) + (x[0] == 0)) * np.linalg.norm(x)`. El comentario adyacente menciona explícitamente el manejo del caso `x_1 == 0`. Notebook sigue siendo nbformat-válido. Ningún otro cell modificado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar scipy.linalg.solve_triangular con back-substitution manual en cell 18</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (estado actual de cell 18 y cell 19 para confirmar variables consumidas)
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md (decisión "solve_triangular en Parte 1")
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md (sección "Pattern: Manual Back-Substitution", "Code Examples" y "Pitfall 5")
  </read_first>
  <action>
    Reemplazar el contenido COMPLETO de **cell 18** del notebook por el siguiente código exacto. Esto: (a) elimina el `from scipy.linalg import solve_triangular`, (b) define un helper `back_substitution` inline en la primera parte del cell, y (c) usa ese helper para resolver ambos sistemas R c = Q^T b. Las variables de salida que cell 19 consume (`Q_mgs, R_mgs, c_mgs, superficie_mgs, restaurada_mgs, Q_hh, R_hh, c_hh, superficie_hh, restaurada_hh`) deben permanecer con los mismos nombres.

    Contenido nuevo y exacto de cell 18:

    ```python
    # Sustitución hacia atrás manual (sin scipy.linalg.solve_triangular)
    # Resuelve R x = b para R triangular superior n x n.
    def back_substitution(R, b):
        """Sustitución hacia atrás para sistema triangular superior R x = b.

        Avanzamos de la última fila hacia la primera porque cada x[i] depende
        únicamente de las componentes ya resueltas x[i+1], ..., x[n-1].
        El producto R[i, i+1:] @ x[i+1:] es una combinación lineal numérica
        equivalente a un bucle anidado pero O(n) por fila gracias a NumPy.
        """
        n = len(b)
        x = np.zeros(n)
        for i in range(n - 1, -1, -1):
            x[i] = (b[i] - R[i, i + 1:] @ x[i + 1:]) / R[i, i]
        return x

    # ── MGS ─────────────────────────────────────────────────────────────────────
    print('Factorizando A con MGS...')
    Q_mgs, R_mgs = qr_mgs(A)
    # Resolver R_hat * c = Q_hat^T * b mediante sustitución hacia atrás manual
    c_mgs = back_substitution(R_mgs, Q_mgs.T @ b_p1)
    superficie_mgs = (A @ c_mgs).reshape(alto, ancho)
    restaurada_mgs = np.clip(imagen_danada - superficie_mgs + 1.0, 0, 1)

    # ── Householder ──────────────────────────────────────────────────────────────
    print('Factorizando A con Householder...')
    Q_hh, R_hh = qr_householder(A)
    c_hh = back_substitution(R_hh, Q_hh.T @ b_p1)
    superficie_hh = (A @ c_hh).reshape(alto, ancho)
    restaurada_hh = np.clip(imagen_danada - superficie_hh + 1.0, 0, 1)

    print('Listo.')
    ```

    Notas:
    - Eliminar `from scipy.linalg import solve_triangular` por completo de cell 18 (era la primera línea).
    - NO añadir cell nuevo: el helper `back_substitution` vive dentro de cell 18.
    - NO modificar cell 19 (visualización 4-panel) ni cell 20 (normas de ortogonalidad). Los nombres de variables que cell 19 lee (`restaurada_mgs`, `restaurada_hh`, etc.) son idénticos.
    - NO tocar el cell 18 de Parte 2 (cell 34) ni la importación de scipy en Parte 2 — Parte 2 sí puede usar `solve_triangular` (es out-of-scope para este plan).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const nb=JSON.parse(fs.readFileSync('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb','utf8')); const s=Array.isArray(nb.cells[18].source)?nb.cells[18].source.join(''):nb.cells[18].source; if(s.includes('from scipy.linalg import solve_triangular')) throw new Error('cell 18 still imports solve_triangular'); if(s.includes('solve_triangular(')) throw new Error('cell 18 still calls solve_triangular'); if(!s.includes('def back_substitution')) throw new Error('cell 18 missing back_substitution helper'); if(!s.includes('back_substitution(R_mgs, Q_mgs.T @ b_p1)')) throw new Error('cell 18 not calling back_substitution for MGS'); if(!s.includes('back_substitution(R_hh, Q_hh.T @ b_p1)')) throw new Error('cell 18 not calling back_substitution for Householder'); if(!s.includes('restaurada_mgs') || !s.includes('restaurada_hh')) throw new Error('output variables missing'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - cell 18 NO contiene la cadena `from scipy.linalg import solve_triangular`
    - cell 18 NO contiene la cadena `solve_triangular(` en ninguna parte
    - cell 18 contiene la cadena `def back_substitution(R, b):`
    - cell 18 contiene la cadena `back_substitution(R_mgs, Q_mgs.T @ b_p1)`
    - cell 18 contiene la cadena `back_substitution(R_hh, Q_hh.T @ b_p1)`
    - cell 18 contiene los nombres `restaurada_mgs` y `restaurada_hh` (consumidos por cell 19)
    - cell 18 contiene los nombres `superficie_mgs` y `superficie_hh`
    - cells 19, 20, 21 byte-idénticas a su estado previo
    - El docstring de `back_substitution` está en español
    - Notebook sigue siendo nbformat-válido: `python -c "import nbformat; nbformat.read('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb', 4)"` retorna sin error
  </acceptance_criteria>
  <done>
    cell 18 ya no depende de scipy.linalg en Parte 1; usa `back_substitution` manual definida inline; produce las mismas variables de salida (Q_mgs, R_mgs, c_mgs, superficie_mgs, restaurada_mgs, Q_hh, R_hh, c_hh, superficie_hh, restaurada_hh). cell 34 de Parte 2 sigue intacto.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ampliar el análisis Markdown del Inciso 2 (cell 11) con κ(A^T A) = [κ(A)]^2 y cuantificación de dígitos</name>
  <files>Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb</files>
  <read_first>
    - Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb (estado actual de cell 11 — el contenido existente debe preservarse, NO reescribirse)
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-CONTEXT.md (decisión "Análisis Markdown — Inciso 2")
    - .planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-RESEARCH.md (sección "Pattern: Inciso 2 Analysis Expansion", "Code Examples" texto exacto, y "Pitfall 4")
  </read_first>
  <action>
    Ampliar (NO reescribir) cell 11 del notebook. El contenido actual debe preservarse íntegro; se debe APPENDAR un nuevo párrafo Markdown al final del source de cell 11. El párrafo debe ser exactamente el siguiente texto en español, manteniendo el formato LaTeX:

    Texto a appendar (precedido por una línea en blanco para separar del párrafo existente):

    ```markdown

    La relación $\kappa(A^\top A) = [\kappa(A)]^2$ explica por qué el método es computacionalmente catastrófico incluso cuando funciona algebraicamente: elevar al cuadrado el número de condición duplica la cantidad de dígitos significativos consumidos. En precisión doble disponemos de aproximadamente $-\log_{10}(\varepsilon_\text{máq}) \approx 16$ dígitos. Si $\kappa(A^\top A) \approx 10^{30}$, se pierden 30 dígitos — es decir, la solución no tiene **ningún** dígito correcto. La impresión en la celda anterior lo confirma numéricamente.
    ```

    Reglas de manipulación:
    - Leer cell 11 source actual.
    - El contenido existente (los dos párrafos: "Las **ecuaciones normales** son el enfoque algebraicamente correcto..." y "La **cancelación catastrófica** ocurre cuando se restan dos números casi iguales...") DEBE permanecer intacto, palabra por palabra.
    - Appendar el nuevo párrafo al final, separado por una línea en blanco.
    - Preservar la estructura de `source` como lista de strings (cada string termina en `\n` excepto opcionalmente el último).

    NO tocar cell 7 (análisis Inciso 1), NO tocar cell 21 (conclusión Inciso 4 — locked en CONTEXT.md como "no cambiar").
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const nb=JSON.parse(fs.readFileSync('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb','utf8')); const s=Array.isArray(nb.cells[11].source)?nb.cells[11].source.join(''):nb.cells[11].source; if(!s.includes('Las **ecuaciones normales** son el enfoque algebraicamente correcto')) throw new Error('cell 11 perdio contenido original (parrafo 1)'); if(!s.includes('La **cancelación catastrófica** ocurre cuando se restan dos números casi iguales')) throw new Error('cell 11 perdio contenido original (parrafo 2)'); if(!s.includes('\\\\kappa(A^\\\\top A) = [\\\\kappa(A)]^2')) throw new Error('cell 11 falta la formula kappa(AtA)=[kappa(A)]^2 en LaTeX'); if(!s.includes('\\\\approx 16')) throw new Error('cell 11 falta cuantificacion ~16 digitos'); if(!s.includes('se pierden 30 dígitos')) throw new Error('cell 11 falta cuantificacion 30 digitos perdidos'); if(!s.includes('ningún')) throw new Error('cell 11 falta enfasis en ningun digito correcto'); if(nb.cells[11].cell_type !== 'markdown') throw new Error('cell 11 cambio de tipo'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - cell 11 sigue siendo `cell_type: markdown`
    - cell 11 contiene la cadena literal `Las **ecuaciones normales** son el enfoque algebraicamente correcto` (párrafo 1 original preservado)
    - cell 11 contiene la cadena literal `La **cancelación catastrófica** ocurre cuando se restan dos números casi iguales` (párrafo 2 original preservado)
    - cell 11 contiene la cadena LaTeX `\kappa(A^\top A) = [\kappa(A)]^2`
    - cell 11 contiene la cadena `\approx 16` (referencia a 16 dígitos de doble precisión)
    - cell 11 contiene la cadena `\kappa(A^\top A) \approx 10^{30}`
    - cell 11 contiene la cadena `se pierden 30 dígitos`
    - cell 11 contiene la cadena `ningún` (con tilde) en negrita Markdown (`**ningún**`)
    - cell 11 contiene la cadena `La impresión en la celda anterior lo confirma numéricamente.`
    - cell 7 y cell 21 byte-idénticos a su estado previo
    - Notebook sigue siendo nbformat-válido
  </acceptance_criteria>
  <done>
    cell 11 ahora menciona explícitamente el teorema κ(A^T A) = [κ(A)]^2, cuantifica los ~16 dígitos disponibles en precisión doble, identifica los 30 dígitos perdidos cuando κ(A^T A) ≈ 10^30, y enfatiza que no queda ningún dígito correcto. El contenido previo (dos párrafos) se preserva sin alteraciones.
  </done>
</task>

</tasks>

<verification>
Después de las 3 tasks, ejecutar `git diff --stat Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` y verificar que solo ese archivo cambió. Inspeccionar el diff con `git diff Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb` para confirmar que los únicos cambios involucran cells 11, 16, 18, 32 — ningún otro cell tocado.

Validación nbformat:
```bash
python -c "import nbformat; nb=nbformat.read('Practica3-AlgComp-Gallon-Montoya-Gomez.ipynb', 4); print(f'Cells: {len(nb.cells)}'); print('Notebook valid')"
```
</verification>

<success_criteria>
- Las 3 tasks completadas con todos los acceptance_criteria pasando
- Notebook sigue siendo nbformat-válido
- Solo cells 11, 16, 18, 32 modificadas; cells 0–10, 12–15, 17, 19–31, 33–38 intactas
- Listo para que plan 02-02 ejecute Restart & Run All y verificación visual
</success_criteria>

<output>
After completion, create `.planning/phases/02-parte-1-rescate-del-patrimonio-documental/02-01-SUMMARY.md` documentando:
- Diff exacto aplicado a cada cell (11, 16, 18, 32)
- Confirmación de que ninguna otra cell fue modificada
- Estado nbformat-válido
- Próximo paso: ejecutar plan 02-02 para Restart & Run All + verificación visual
</output>
